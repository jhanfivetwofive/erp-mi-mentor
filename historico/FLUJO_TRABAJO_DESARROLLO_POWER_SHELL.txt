$env:FLASK_SECRET_KEY = "f9b3dd7a758a83c10b4ae43db3a472a887f50595c31c9e2dbe6be3426fd9d0f3"
$env:GCP_PROJECT_ID = "fivetwofive-20"

🧪 Flujo típico de trabajo
1. Desarrollo

git checkout dev
git add .
git commit -m
git push origin dev

gcloud run deploy mi-app-dev `
  --source . `
  --region us-central1 `
  --project fivetwofive-20


➡️ Esto activa el despliegue automático a mi-app-dev.

2. Paso a producción

git checkout main
git pull origin main          # actualiza main
git merge dev                 # trae cambios de dev a main
# si hay conflictos: resuélvelos, luego:
git add .
git commit -m "Merge dev into main (release prod)"
git push origin main

gcloud run deploy mi-app-prod `
  --source . `
  --region us-central1 `
  --project fivetwofive-20


3.- regresar a dev

git checkout dev
git pull origin dev
# (opcional) sincronizar dev con lo último de main:
git merge main
git push origin dev


🧰 Tip en VS Code
Presiona:

scss
Copiar
Editar
Ctrl + A   (Seleccionar todo)
Ctrl + K, Ctrl + F   (Autoformatear indentación)
O en menú: Click derecho > Format Document.

gcloud beta run jobs run facebook-etl-job --region=us-central1
gcloud beta run jobs describe facebook-etl-job --region=REGION --format yaml
gcloud run deploy mi-app-dev \`
  --image gcr.io/fivetwofive-20/mi-app-dev \`
  --platform managed \`
  --region us-central1 \`
  --allow-unauthenticated

Google 
project-306311554573
